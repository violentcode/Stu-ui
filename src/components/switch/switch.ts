export interface ISwitchProps {
  size: string | number
  activeColor: string
  inactiveColor: string
}

export const switchProps: ISwitchProps = {
  size: '26px',
  activeColor: '#1989fa',
  inactiveColor: 'rgba(120, 120, 128, 0.16)'
}
